# Claude Code

Follow **AGENTS.md** as the project source of truth.

This is Foundry: a Next.js 15 App Router multi-agent system for SMEs. Conductor + specialists live in `src/lib/agents`. The console streams SSE from `POST /api/agents/run`.

When adding an agent, follow `.cursor/skills/add-specialist-agent/SKILL.md`.
